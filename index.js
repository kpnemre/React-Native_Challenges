/**
 * @format
 */

import {AppRegistry} from 'react-native';
import Todolist from './Course_Life_Cycle'
import {name as appName} from './app.json';

AppRegistry.registerComponent(appName, () => Todolist);
